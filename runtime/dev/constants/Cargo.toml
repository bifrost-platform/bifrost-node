[package]
name = "bifrost-dev-constants"
version = "1.0.0"
description = "The runtime constants for Bifrost dev chain"
authors = { workspace = true }
homepage = { workspace = true }
edition = { workspace = true }
license = { workspace = true }
repository = { workspace = true }

[dependencies]
# Bifrost
bifrost-common-constants = { workspace = true }

# Substrate
frame-support = { workspace = true }

[features]
default = ["std"]
std = ["bifrost-common-constants/std", "frame-support/std"]
